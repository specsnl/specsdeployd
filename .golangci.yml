version: "2"
linters:
  default: standard
  enable:
    - wsl_v5
    - whitespace
    - modernize
  settings:
    errcheck:
      # Reporting a failed write would itself need a working stream.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintln
        - fmt.Fprintf
